<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/default_padding">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/todo_item_title_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/welcome_prompt_text_size"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Todo Title" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/todo_item_content_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        app:layout_constraintTop_toBottomOf="@id/todo_item_title_text_view"
        tools:text="This is just a sample content view" />

</androidx.constraintlayout.widget.ConstraintLayout>